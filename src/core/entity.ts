/**
 * Base interface for any object or character that can exist within a Compartment.
 * Can be expanded later with properties like ID, type, etc.
 */
export interface Entity {
    name: string;
    // Add common properties for entities here later
}